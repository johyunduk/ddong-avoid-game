Implement the following plan:

# 신규 유저 5000 SKOR 웰컴 보너스

## Context
처음 게임에 접속하는 유저에게 SKOR 5000개를 무료 지급.
신규 유저는 `user_skor` 테이블에 행이 없으므로, 첫 잔액 조회(`getSkorBalance`) 시 이를 감지하여 5000으로 초기화.

---

## 수정 파일
- `src/utils/skor.ts` — `getSkorBalance()` 내 신규 유저 분기 수정

---

## 구현

### 현재 코드
```typescript
// data가 null이면 신규 유저 → balance = 0 반환
const balance = (data?.balance as number) ?? 0;
cacheSkorBalance(balance);
return balance;
```

### 변경 후
```typescript
if (!data) {
  // 신규 유저: 5000 SKOR 웰컴 보너스로 초기화
  const WELCOME_BONUS = 5000;
  await supabase.from('user_skor').insert({
    user_id: userId,
    balance: WELCOME_BONUS,
    weekly_earned: 0,
  });
  cacheSkorBalance(WELCOME_BONUS);
  return WELCOME_BONUS;
}
const balance = data.balance as number;
cacheSkorBalance(balance);
return balance;
```

### 중복 지급 방지
- 두 번째 접속부터는 `data`가 null이 아니므로 분기에 진입하지 않음
- Supabase RLS로 자신의 row만 INSERT 가능
- `weekly_earned: 0` 명시로 주간 한도에 영향 없음

---

## 검증
- 첫 접속 시 ModeSelectScene에서 `💰 5000 SKOR` 표시 확인
- 재접속 시 기존 잔액 유지 확인 (5000 중복 지급 없음)
- Supabase Dashboard → `user_skor` 테이블에서 신규 행 `balance=5000` 확인


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/johyunduk/.REDACTED.jsonl

---

user_skor 에서 {code: "42501", details: null, hint: null,…}
code
: 
"42501"
details
: 
null
hint
: 
null
message
: 
"new row violates row-level security policy for table \"user_skor\"" 이 에러는 왜 뜨는거지?

---

{code: "42501", details: null, hint: null,…}
code
: 
"42501"
details
: 
null
hint
: 
null
message
: 
"new row violates row-level security policy for table \"user_skor\"" 이거 계속 뜨는데?

---

메뉴에 안보이는데 최신화 해서 알려줄래?

---

저거 쿼리 success 떴는데도 403 에러 그대로 나는데?

---

[SKOR] 신규 유저 감지, userId: 7dffa262-57a6-4f8d-83dc-d9c5f1918702 session: 
[SKOR] INSERT 결과 error: null

---

그래서 내가 뭘 해줘야 하는데?

---

됐네 이거 깃 푸시 해줘

---

다 같이 올려.

---

자 그러면 이제 문서들 구현 완료된거 그리고 최신화 안된거 그런거 정리하번 해줘. 그리고 이제 구현 todo 리스트에는 뽑기 중복 경험치화 해서 캐릭터 성장 하나랑 스코르 획득을 위한 퀘스트 시스템 추가야 이렇게 정리한번 해줘

---

문서 최신화 작업 먼저 해줘.

---

10회 뽑기 건너뛰기가 이상함. sr 이상 아닌 경우에도 건너뛰기 활성화 필요

---

가끔 정상 플레이 인데도 [Anti-cheat] timeScale 조작 감지: 0.93 가 뜸

---

이렇게 깃 푸시해줘.

---

아 그리고 처음 게임 플레이를 할 때 스코르 5000개 주던거 10000개로 바꿔줘

---

뽑기화면 진입 했을 때 레거시만 보이는데 센티넬도 보이게 해서 슬라이드 형식으로 만들어줄 수 있나?

---

[Request interrupted by user for tool use]

---

저기에 SR 애들도 슬라이드에 추가하자. 순서는 센티넬, 레거신, sr은 아무렇게나 해줘. 그리고 2초 단위로 자동으로 넘어가면 좋겠어

---

[Request interrupted by user for tool use]

---

뒤에 배경 일러스트들만 옆으로 스르륵 넘어가게 할 순 없나? 깜빡깜빡 거리는게 불편한데

---

logout

---

[Request interrupted by user]