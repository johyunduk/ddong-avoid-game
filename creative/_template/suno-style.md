# Suno Style Prompt

**Title**

```
<곡 제목>
```

**Style of Music** (Suno 의 Style 칸에 붙여넣기, 쉼표 구분 200자 내외)

```
korean pop, <mood>, <vocal>, <bpm> BPM, <instruments>, <atmosphere>
```

**Exclude Styles**

```
harsh distortion, screaming
```

> spec.yaml 의 `music:` 섹션에서 파생한다. 값이 서로 어긋나면 spec.yaml 이 우선이다.
