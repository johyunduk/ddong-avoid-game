Implement the following plan:

# 플랜: 미보유 캐릭터 로컬스토리지 변조 방어

## Context
`getSelectedCharacter()`는 localStorage를 검증 없이 그대로 읽으며, `GameScene.init()`도 소유 여부를 확인하지 않는다.
→ 브라우저 콘솔에서 `localStorage.setItem('selectedCharacter', 'legacy')` 한 줄로 미보유 UR 캐릭터의 능력(피버 타임, 점수 1.2배 등)을 무료로 사용할 수 있다.

## 수정 범위 (2개 파일)

### 1. `src/utils/character.ts`
`getOwnedCharacters()`와 `getSelectedCharacter()`를 합쳐 소유 여부를 교차 검증하는 함수 추가:

```typescript
/** 소유 목록과 교차 검증하여 안전한 캐릭터 ID 반환. 미보유면 'chibi'. */
export function getSafeSelectedCharacter(): string {
  const selected = getSelectedCharacter();
  const owned = getOwnedCharacters();
  if (owned.includes(selected)) return selected;
  // 변조 감지 → chibi로 강제 초기화
  setSelectedCharacter('chibi');
  return 'chibi';
}
```

### 2. `src/scenes/GameScene.ts`
`init()` 메서드에서 호출부 교체:

```typescript
// 변경 전
import { getSelectedCharacter } from '../utils/character';
this.selectedCharId = getSelectedCharacter();

// 변경 후
import { getSafeSelectedCharacter } from '../utils/character';
this.selectedCharId = getSafeSelectedCharacter();
```

## 수정하지 않는 것
- `CharacterSelectScene` — 이미 UI 레벨에서 소유 여부로 카드 잠금 표시함
- `ownedCharacters` localStorage 자체 — 클라이언트 게임 특성상 완전한 방어는 불가, 선택 캐릭터 검증만으로 충분

## 검증
1. 브라우저 콘솔에서 `localStorage.setItem('selectedCharacter', 'legacy')` 실행
2. 게임 시작 → chibi로 fallback 되는지 확인 (미보유 캐릭터 차단)
3. 정상 보유 캐릭터는 그대로 사용 가능한지 확인
4. `npx tsc --noEmit` 타입 오류 없는지 확인


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/johyunduk/.REDACTED.jsonl

---

현재 캐릭터를 이미지가 사이즈들이 전부 다른가?

---

얘네 전부 백업해두고 통일 시켜줄 수 있어?

---

[Request interrupted by user for tool use]